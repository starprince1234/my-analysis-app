# Flutter compile-time configuration.
# Copy values into your local run command with --dart-define.

# URL of the Flask/Socket.IO backend from the my-analysis-app main branch.
# For local backend development, the Flask service usually listens on port 5000.
BACKEND_URL=http://localhost:5000
